[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "mark-xl-rust"
version = "0.1.0"
description = "Rust extension module for Mark-XL (vendored fork of OpenJarvis)"
requires-python = ">=3.11"

[tool.maturin]
manifest-path = "Cargo.toml"
